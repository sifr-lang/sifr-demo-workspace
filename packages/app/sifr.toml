[package]
name = "workspace_app"
edition = "2026"
sifr-version = ">=0.3,<0.4"

[source]
roots = ["sifr"]

[exports]
modules = ["app"]

[trust]
native = ["backend-utils"]
build-scripts = []
proc-macros = []
